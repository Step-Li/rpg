export interface IFilters {
    evaluationRange?: number | number[];
    maxEvaluation?: number;
    years?: string[];
    nomination?: string;
    adventureType?: string | null;
    systems?: string[];
}
